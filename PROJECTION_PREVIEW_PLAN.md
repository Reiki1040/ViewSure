# プロジェクター投影プレビュー機能 実装計画書

## 1. 目的

本機能は、PCのモニター上で見ているPDF資料が、プロジェクターで投影された際にどのように見えるかをシミュレーションし、プレビュー画面を提供することを目的とする。

特に、プロジェクターに特有の色の再現範囲（色域）やコントラストの変化を擬似的に再現することで、発表前に配色や視認性の問題を事前に確認できるようにする。

## 2. 中心的な実装アプローチ

**WebAssembly (WASM) を利用したピクセルデータ変換方式**

本プロジェクトには、`public/wasm/tone_mapping.wasm` というバイナリファイルが同梱されている。これは、画像データの色調を高速に変換するために最適化された WebAssembly モジュールであると推測される。

このWASMモジュールを活用し、以下の処理フローで機能を実現する。

1.  PDFの特定ページを、一旦メモリ上の非表示キャンバスに描画する。
2.  描画されたキャンバスからピクセルデータ (`ImageData`) を抽出する。
3.  抽出したピクセルデータを `tone_mapping.wasm` モジュールに渡し、プロジェクターの色調をシミュレートする変換処理を実行する。
4.  WASMによって変換されたピクセルデータを、ユーザーに見えている表示用キャンバスに描き戻す。

このアプローチにより、既存のPDFレンダリング処理（`pdfjs-dist`）を活かしつつ、表示結果にのみ変更を加えるため、元のPDFファイルを破損するリスクなく、安全にプレビュー機能を実現できる。

## 3. 主要な技術要素

- **`tone_mapping.wasm`**: 色調変換処理の心臓部。JavaScript側からこのモジュールを呼び出して使用する。
- **React State**: プレビュー機能のON/OFF状態を管理する (`const [isPreviewEnabled, setIsPreviewEnabled] = useState(false);`)。
- **Canvas API**:
  - `getContext('2d').getImageData()`: キャンバスからピクセルデータを取得する。
  - `getContext('2d').putImageData()`: 変換後のピクセルデータをキャンバスに描画する。
- **React `useEffect`**: プレビュー機能のON/OFFが切り替えられた際に、表示中のページを即座に再描画するために使用する。

## 4. 実装ステップ

以下の4ステップで実装を進める。

### ステップ1: WASMモジュールの調査と読み込み【最優先】

機能実装の核となる `tone_mapping.wasm` の仕様を解明する。

- **作業内容:**
  1.  `fetch` APIを使用して `public/wasm/tone_mapping.wasm` をバイナリデータとして読み込む。
  2.  `WebAssembly.instantiate` を使ってWASMモジュールをインスタンス化する。
  3.  インスタンスの `exports` プロパティを `console.log` で出力し、JavaScriptから呼び出し可能な関数の一覧と、その名前を特定する。
  - *(関数のシグネチャ（引数や返り値）も、名前から推測するか、簡単なテストで検証する)*

### ステップ2: UIの追加と状態管理

プレビュー機能のON/OFFを切り替えるためのUIを実装する。

- **作業内容:**
  1.  `App.tsx` に、プレビュー機能の有効状態を保持する `isPreviewEnabled` stateを追加する。
  2.  `studio-command-bar` コンポーネント（フッターの操作バー）内に、トグルスイッチやボタンなどのUI要素を追加し、クリックすると `isPreviewEnabled` の状態が切り替わるようにする。

### ステップ3: レンダリング処理の拡張

`App.tsx` 内の `renderPage` 関数を修正し、プレビュー状態に応じて描画処理を分岐させる。

- **作業内容:**
  1.  **プレビューOFFの場合 (現状のまま):** `pdf.js` が描画したキャンバスを `drawImage` でそのまま表示する。
  2.  **プレビューONの場合 (新規実装):**
      a.  非表示キャンバスから `getImageData` でピクセルデータを取得。
      b.  WASMモジュールにピクセルデータを渡し、変換処理を実行。
      c.  返ってきた変換後のピクセルデータを `putImageData` で表示用キャンバスに描画する。

### ステップ4: 再描画処理の実装

プレビューのON/OFFが切り替わった際に、表示が即座に更新されるようにする。

- **作業内容:**
  1.  `App.tsx` に `useEffect` を追加し、`isPreviewEnabled` の変更を監視する。
  2.  `isPreviewEnabled` が変更されたら、`useEffect` の中で `renderPage(currentPage)` を呼び出し、現在のページを再描画させる。

## 5. 最初の具体的なアクション

上記計画に基づき、まずは**「ステップ1: WASMモジュールの調査と読み込み」**に着手する。`tone_mapping.wasm` の内部仕様が不明である限り、後続のステップを進めることができないため、これを最優先タスクとする。

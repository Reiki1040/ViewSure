# ViewSure：メイン処理の技術詳細解説

ViewSureがPDFをどのように読み込み、どのような計算で「見やすさ」を判定しているのか、その具体的な処理フローを解説します。

---

## 1. PDFレンダリング・フロー（読み込みから表示まで）

ユーザーがファイルをアップロードしてから画面に表示されるまでの裏側です。

1.  **Workerスレッドの起動:** PDFの解析は重いため、ブラウザのメインの動きを止めないよう `Web Worker`（別動隊）で処理を開始します。
2.  **デジタル情報の抽出:** `pdf.js` を使い、PDFから「どこにどの文字があるか」「どんな画像が埋まっているか」というデータを読み取ります。
3.  **キャンバスへの描画:** 読み取ったデータを、ブラウザ上の目に見えない「作業用キャンバス（Canvas）」に高解像度で描き出します。これが解析の土台になります。

---

## 2. コントラスト解析のアルゴリズム（一番の目玉機能）

「白飛びして文字が見えない」を科学的に判定する仕組みです。

1.  **座標の特定:** 手順1で取得した「文字の位置情報」を元に、キャンバス上の特定の数ピクセルをサンプリングします。
2.  **色の抽出:**
    *   **文字色:** テキストデータから色情報を取得。
    *   **背景色:** 文字が描かれている位置の「すぐ下のピクセル」の色を直接調査します。
3.  **相対輝度の計算:** 
    *   人間が感じる「明るさ」の基準（緑が明るく感じ、青が暗く感じる特性）に基づき、色（RGB）を「輝度」という数値に変換します。
4.  **比率の算出:** `(明るい方の輝度 + 0.05) / (暗い方の輝度 + 0.05)` という数式でコントラスト比を出します。
    *   **判定:** この比が **4.5 : 1** 未満なら「読みにくい」とフラグを立てます。

---

## 3. 配色バランス解析（黄金比のチェック）

「デザインがごちゃついていないか」を面積で判定します。

1.  **全ピクセルのスキャン:** ページの全ピクセル（数百万個）の色を1つずつチェックします。
2.  **色のグループ化:** 似たような色は同じグループとしてまとめ、出現頻度の高い順に並べます。
3.  **面積比の算出:**
    *   一番多い色 ＝ **ベースカラー**（目標70%）
    *   二番目に多い色 ＝ **メインカラー**（目標25%）
    *   それ以外の目立つ色 ＝ **アクセントカラー**（目標5%）
4.  **バランス判定:** ベースが多すぎたり、アクセントが強すぎたりする場合に「バランスが崩れています」と通知します。

---

## 4. プロジェクター・シミュレーション（WASM処理）

実際のプロジェクターの「性能限界」を再現する高度な処理です。

1.  **ピクセルデータの転送:** JavaScript上の画像データを、高速計算が得意な `WebAssembly(WASM)` 領域へ送り込みます。
2.  **ブラックレベル補正（黒浮き）:**
    *   プロジェクターは光で映像を出すため、真っ黒な部分でもわずかに光が漏れます。
    *   WASM内で、すべてのピクセルの「暗さの下限」を底上げし、画面全体を少し白ませます。
3.  **ガンマ補正:** 中間色の明るさを調整し、プロジェクター特有の「色が薄くなる現象」を再現します。
4.  **再描画:** 計算し直されたデータをキャンバスに戻し、ユーザーに「実際の会場での見え方」として提示します。

---

## 5. 画像抽出と二次解析

PDF内の写真やイラストだけを取り出す処理です。

1.  **埋め込みオブジェクトの走査:** PDFのデータ構造をスキャンし、`Image` オブジェクトを見つけ出します。
2.  **切り出し:** 見つかった画像を独立した小さなキャンバスにコピーします。
3.  **再解析:** 切り出した画像に対してもコントラストチェックを行い、図解の中の文字が読めるかどうかを検証します。
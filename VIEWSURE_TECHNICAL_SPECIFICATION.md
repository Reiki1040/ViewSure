# ViewSure テクニカル・オーバービュー

ViewSureは、プレゼンテーション資料（PDF）の視認性をアクセシビリティ（WCAG）の観点から自動解析し、プロジェクター投影時の見え方をシミュレートする高度なWebアプリケーションです。

## 1. システムアーキテクチャ
本アプリはReactをベースに、重い画像処理やPDF操作をブラウザ上で完結させるため、WebAssembly（WASM）とWeb Workersを活用しています。

- **Frontend:** React (TypeScript) / Vite
- **PDF Rendering:** `pdf.js` (WebWorker経由での非同期描画)
- **Image Processing:** WebAssembly (WASM) によるトーンマッピング処理
- **Analysis Logic:** 純粋なJavaScript/TypeScriptによるピクセルレベルのコントラスト解析

---

## 2. コア・テクノロジーと主要ロジック

### A. 自動アクセシビリティ解析 (`handleReadabilityCheck`)
アップロードされたPDFの各ページに対して、以下の4つの主要な解析を並列または逐次実行します。

1. **コントラスト解析 (`analyzePageContrast`)**
   - 描画されたキャンバスからテキスト領域を特定。
   - テキストとその直下の背景色の輝度（Luminance）を計算。
   - WCAG基準（AA: 4.5:1, AAA: 7:1）に基づき、視認性が低い箇所をリストアップ。

2. **タイポグラフィ解析**
   - PDFのメタデータからフォントサイズ（pt）を抽出。
   - プロジェクター投影時の視認限界を考慮し、18pt未満を「小さい文字」として警告。

3. **配色バランス解析 (`analyzeColorBalance`)**
   - ページ内の全ピクセルの色分布をヒストグラム化。
   - 「ベース(70%)：メイン(25%)：アクセント(5%)」の黄金比からの逸脱を計算。

4. **原色検出 (`detectPrimaryColors`)**
   - 赤(#FF0000)や青(#0000FF)などの高彩度な原色が広い面積を占めていないか確認。
   - プロジェクター特有の「ハレーション（眩しさ）」のリスクを判定。

### B. プロジェクター・プレビュー (`applyToneMapping`)
実際のプロジェクターが持つ「物理的な限界」をシミュレートします。
- **黒浮きの再現:** プロジェクターでは「完全な黒（0,0,0）」を出せず、常にバックライトの漏れが発生します。WASMモジュールはこの最小輝度（オフセット）を加算します。
- **コントラスト圧縮:** 会場が明るい場合や性能が低い場合のコントラスト低下をシミュレートし、資料の耐久性を検証します。

### C. 高速画像抽出 (`extractImagesFromPdf`)
- PDF内のベクトルデータではなく、埋め込まれたラスター画像（写真や図解）を抽出し、個別にコントラストをチェックできるギャラリー機能を備えています。

---

## 3. UI/UX 設計
- **没入型ランディング:** ダークテーマによる先進的な印象付け。
- **解析スタジオ:** ライトテーマによる作業効率化。60/40の厳格なGridレイアウトにより、左側にプレビュー、右側に解析レポートを配置し、視線の移動を最小限に抑えています。
- **レスポンシブ:** `clamp()`関数による流体的なタイポグラフィと、モバイルでの縦積みレイアウトへの柔軟な切り替え。

---

## 4. プロジェクト構造

```text
src/
├── components/          # UIコンポーネント (Uploader, Viewport等)
├── hooks/               # PDFレンダリング等のロジック切り出し
├── utils/               # 解析エンジン、WASM連携、WCAG計算
│   ├── wcag.ts          # コントラスト・配色解析のコア
│   ├── toneMapping.ts   # プロジェクターシミュレーション連携
│   └── imageExtractor.ts # PDF内画像抽出
└── styles.css           # 厳格なデザイン定義 (Reiki's Custom)
```

---

## 5. 今後の拡張性
- **自動修正（Auto-Fix）:** 解析された問題（コントラスト不足等）に対し、代替色を提案または自動適用する機能の基盤が用意されています。
- **リアルタイム解析:** ページをめくるごとにバックグラウンドで解析を継続する非同期処理構造。

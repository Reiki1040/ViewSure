# ViewSure プログラム説明
ViewSureは「プロジェクション準備支援アプリ」です。プレゼンテーション資料をプロジェクターで投影する前に、見やすさを調整したり、アクセシビリティ（誰でも見やすいか）をチェックしたりするツールです。

## 全体の仕組み

ViewSureは大きく分けて4つの部分で動いています：

1. **ファイル読み込み部分** - PDFや画像を読み込む
2. **表示部分** - 読み込んだファイルを画面に表示する
3. **調整部分** - 明るさやコントラストを調整する
4. **エクスポート部分** - 調整した結果をPDFで保存する

## 主要なプログラムファイルとその役割

### 1. App.tsx - アプリケーションの心臓部

**役割**: アプリ全体をまとめるメインコンポーネント

**主な処理**:
- 画面の表示切り替え（ランディング画面 ↔ 編集画面）
- ユーザーの操作（ボタンクリックなど）を受け取る
- 他の部分（ファイル読み込み、表示、調整）を連携させる
- エラーメッセージやローディング表示を管理

**簡単に言うと**: アプリ全体の指揮者のような役割です。

### 2. useAppState.ts - 状態を覚えておく部分

**役割**: アプリの状態（今どうなっているか）を記憶する

**記憶しているもの**:
- 今どのファイルを開いているか
- 明るさとコントラストの設定値
- 今表示しているページ番号
- エラーメッセージやローディング状態

**簡単に言うと**: アプリの「記憶」を担当する部分です。

### 3. useProjectionRenderer.ts - 画像を表示する部分

**役割**: WebGLを使って高速に画像を表示・調整する

**主な処理**:
- 画像を画面に表示する
- 明るさ・コントラストをリアルタイムで調整
- プロジェクター風の効果を追加
- 画面のキャプチャ（スクリーンショットのようなもの）

**技術的なポイント**:
- WebGLはブラウザで3Dグラフィックスを扱う技術
- 通常の画像表示より高速に処理できる

### 4. FileLoader.ts - ファイルを読み込む部分

**役割**: 様々な形式のファイルを読み込んで統一された形式に変換

**対応ファイル形式**:
- PDFファイル
- PowerPointファイル（.pptx）
- 画像ファイル（PNG, JPEG, WEBP, HEIC）

**主な処理**:
- ファイルの形式を自動判別
- それぞれの形式に合った方法で読み込み
- すべて同じ形式に変換して返す

### 5. ExportService.ts - PDFを作成する部分

**役割**: 調整した画像をPDFファイルとして保存

**主な処理**:
- 現在表示している画像をキャプチャ
- 複数ページの場合はすべてのページを処理
- PDFファイルを作成
- ダウンロードリンクを提供

### 6. useWcagHelper.ts - アクセシビリティをチェックする部分

**役割**: 資料がWCAG（ウェブコンテンツアクセシビリティガイドライン）に沿っているかチェック

**チェック項目**:
- 文字の大きさは十分か
- 文字と背景のコントラスト（差）は十分か
- 見出しと本文の区別はつきやすいか

**主な処理**:
- 自動的に問題点を検出
- 改善提案を表示
- ワンクリックで自動調整

## データの流れ（ユーザーの操作例）

ユーザーが「PDFファイルを開く → 明るさ調整 → PDF保存」する場合のデータの流れ：

1. **ファイル選択**
   - ユーザーがPDFファイルを選択
   - → `FileUploader.tsx`がファイルを受け取る
   - → `FileLoader.ts`がPDFを読み込む
   - → `useAppState.ts`に「今開いているファイル」を記憶

2. **表示**
   - → `useProjectionRenderer.ts`が画像を表示
   - → 画面にPDFの内容が表示される

3. **調整**
   - ユーザーが明るさスライダーを動かす
   - → `App.tsx`が変更を検知
   - → `useProjectionRenderer.ts`に新しい設定値を伝える
   - → 画面の表示がリアルタイムで変わる

4. **保存**
   - ユーザーが保存ボタンをクリック
   - → `ExportService.ts`が現在の表示をキャプチャ
   - → PDFファイルを作成
   - → ダウンロードが開始される

## 重要な概念の説明

### フック（Hook）とは

Reactの機能で、コンポーネントに「状態」や「副作用」を追加するための関数です。

例：
- `useState` - 状態を記憶する
- `useEffect` - 特定のタイミングで処理を実行する
- `useRef` - 再レンダリング間で値を保持する

ViewSureではこれらを組み合わせてカスタムフックを作っています。

### コンポーネント（Component）とは

UIの部品のことです。ボタン、入力欄、画像表示エリアなど、画面を構成するパーツです。

### サービス（Service）とは

特定の機能（ファイル保存など）を担当するクラスや関数の集まりです。UIから独立しているので、同じ機能を様々な場所で使い回せます。

## 開発のヒント

### デバッグのコツ

1. **コンソールログ**: プログラムの動きを追うために`console.log`を使う
2. **React Developer Tools**: ブラウザ拡張でコンポーネントの状態を確認
3. **エラーメッセージ**: エラーが発生したら、メッセージをよく読む

### コードを読むときのポイント

1. **ファイル名から役割を推測**: ファイル名を見ればおおよその役割がわかる
2. **インポート文を確認**: どのファイルがどのファイルを使っているか確認
3. **小さく理解する**: 一度に全部理解しようとせず、部分ごとに理解する

### よく使われる用語

- **状態（State）**: プログラムが記憶しているデータ
- **プロパティ（Props）**: コンポーネント間で受け渡すデータ
- **イベント**: ユーザーの操作（クリック、入力など）
- **レンダリング**: 画面を描画すること
- **エフェクト**: 副作用（データの読み込みなど）

## まとめ

ViewSureは、複雑な処理を小さな部品に分けて、それぞれが単純な役割を持つように設計されています。プログラミング経験が浅くても、1つ1つの部品を理解していけば、全体像が把握できるようになります。

わからないことがあれば、まずは小さな部分から理解を始めてみてください！
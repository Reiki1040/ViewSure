# ViewSure プログラム説明（現行実装）
ViewSure は「プロジェクター投影前の確認を支援する PDF プレビューアプリ」です。  
PDF を読み込み、ページ表示・プロジェクタープレビュー・読みやすさチェックを行います。

## 全体の仕組み

ViewSure は次の 3 つを中心に構成されています。

1. **PDF 読み込み** - pdf.js で PDF を読み込み、ページを Canvas に描画  
2. **表示 / ナビゲーション** - プレビュー表示とページ移動  
3. **読みやすさチェック** - 文字サイズ、行間、情報量、コントラストの簡易判定

## 主要なプログラムファイルと役割

### 1. `src/App.tsx` - アプリの統合制御

**役割**: 画面切替、PDF 読み込み、ページ描画、読みやすさチェックを一括で管理  
**主な処理**:
- ランディング画面 ↔ プレビュー画面の切り替え
- ページ描画とアスペクト比更新
- プロジェクタープレビューの ON / OFF
- 読みやすさチェックの実行と結果表示

### 2. `src/hooks/usePdfRenderer.ts` - PDF レンダラー管理

**役割**: PDF レンダラーの生成・保持・破棄  
**主な処理**:
- `createPdfRenderer` を呼び出して PDF を読み込む
- `dispose` で PDF リソースを解放
- 読み込み中状態やエラーを保持

### 3. `src/utils/pdf.ts` - PDF 描画とテキスト抽出

**役割**: pdf.js の初期化とページ描画・テキスト抽出  
**主な処理**:
- ページ Canvas の生成（キャッシュ付き）
- テキスト位置情報の抽出
- dispose によるリソース破棄

### 4. `src/utils/toneMapping.ts` - プロジェクタープレビュー

**役割**: 投影時の見え方を模擬する画像処理  
**主な処理**:
- WASM モジュールを使った輝度・コントラスト補正
- 解像感の低下を模擬するダウンスケール処理
- WASM 未使用時の JS フォールバック

### 5. `src/utils/wcag.ts` - 読みやすさチェック

**役割**: テキストと背景のコントラスト比を算出し、WCAG 基準に照らして評価  
**主な処理**:
- テキスト領域と背景領域の色サンプル
- WCAG 2.1 AA 基準での判定（通常 4.5:1 / 大きい文字 3:1）

### 6. UI コンポーネント

- `LandingScreen.tsx`: スタート画面
- `FileUploader.tsx`: PDF のドラッグ＆ドロップ / ファイル選択
- `ProjectionViewport.tsx`: Canvas 表示と前後ページ移動ボタン
- `LoadingSpinner.tsx`: 読み込み表示

## データの流れ（ユーザー操作例）

1. **PDF を読み込む**
   - `FileUploader` がファイルを受け取る
   - `usePdfRenderer` が `createPdfRenderer` で PDF を初期化
2. **プレビュー表示**
   - `renderPage` が Canvas に描画
   - 必要に応じて `applyToneMapping` を適用
3. **読みやすさチェック**
   - `getPageTextContent` でテキストを抽出
   - `analyzePageContrast` でコントラストを評価
   - 結果をオーバーレイに表示

## まとめ

ViewSure は「PDF を投影環境でどう見えるか」「読みやすいか」を短時間で確認するための軽量アプリです。  
PDF 描画は `pdfjs-dist`、投影風の色補正は WASM、読みやすさチェックはテキスト抽出と簡易解析で実現しています。

# ViewSure 機能仕様書 (Feature Specifications)

## 1. 概要
本ドキュメントは、ViewSure アプリケーションが提供する機能の詳細仕様を定義します。

## 2. ユーザーフロー
1.  **Landing**: ユーザーはランディング画面から開始する。
2.  **Upload**: PDFファイルをドラッグ＆ドロップまたはファイル選択ダイアログから読み込む。
3.  **Preview**: 読み込まれたスライドを閲覧する。
4.  **Simulation**: 必要に応じて「プロジェクタープレビュー」をONにし、投影時の見え方を確認する。
5.  **Audit**: 自動的に実行される「読みやすさチェック」の結果を確認し、問題のある箇所を特定する。

## 3. 機能詳細

### 3.1 PDF読み込み (File Import)
- **対応フォーマット**: PDF (`.pdf`) のみ。
- **処理方式**: クライアントサイドでのメモリ内読み込み（サーバーへの送信なし）。
- **制限事項**: パスワード保護されたPDFは非対応（エラー表示）。

### 3.2 プレビュー表示 (Preview)
- **レンダリング**: PDF.js を使用し、Canvas要素として描画。
- **ナビゲーション**:
    - 前へ / 次へ ボタン
    - スライダーによるシーク
    - ページ番号直接入力
    - キーボード操作（矢印キー）
- **表示品質**: デバイスのピクセル比（Retina等）を考慮した高解像度レンダリング。

### 3.3 プロジェクターシミュレーション (Projector Simulation)
投影環境特有の画質劣化をシミュレートするモード。トグルスイッチで切り替え可能。

- **画像処理パイプライン**:
    1.  **解像度低下**: 原寸の約70%にダウンスケール後、アップスケールしてぼやけ（ソフトフォーカス）を再現。
    2.  **輝度・コントラスト調整**:
        - 輝度オフセット: 全体的に暗くする（黒浮きの再現）。
        - コントラスト係数: コントラスト比を下げる（0.9倍程度）。
    3.  **環境光 (Haze)**: 画面全体に白の透過レイヤー（約20%）を合成し、部屋の照明によるコントラスト低下を再現。
- **実装**: WebAssembly (WASM) による高速処理を優先し、非対応環境ではJavaScriptで同等の処理を行う。

### 3.4 アクセシビリティ監査 (Readability Audit)
表示中のページに対して、以下の基準で自動チェックを行う。

| 項目 | 判定基準 | 警告内容 |
| :--- | :--- | :--- |
| **文字サイズ** | 18pt 未満 | 小さい文字が含まれる |
| **文字サイズ** | 72pt 超過 | 極端に大きい文字が含まれる |
| **情報量** | 1ページ 400文字 超過 | 情報量が多い |
| **行間** | 行間隔 < 12px (推定) | 行間が詰まり気味 |
| **コントラスト** | WCAG AA基準未満 | コントラスト不足 (比率とテキストを表示) |

#### コントラスト判定の詳細ロジック
- **サンプリング**: テキスト領域とその周辺からピクセルを取得。
- **色抽出**: K-means法（クラスタリング）を用いて、アンチエイリアスによる中間色を除外し、真の「文字色」と「背景色」を推定。
- **計算**: 推定された2色間のコントラスト比を計算。大きい文字（18pt以上）は3:1、通常文字は4.5:1を閾値とする。

## 4. 非機能要件
- **パフォーマンス**:
    - PDFレンダリング結果はキャッシュし、ページ戻り時の再描画を回避。
    - 画像処理などの重いタスクはメインスレッドをブロックしないよう配慮（WASM活用）。
- **セキュリティ**:
    - ローカル完結型のため、機密情報を含む資料でも安全に利用可能。

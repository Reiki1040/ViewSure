# 画像抽出アルゴリズムのまとめ（現行）

## 目的
- 修正ボタンON時にサイドギャラリーへ「見た目そのまま」の画像だけを抽出する。
- 図版内の文字は残しつつ、本文テキストや装飾は拾わない。

## 実装方針と使い分け
- **推奨（高精度）**: `clusterExtractor.ts`  
  - PDFの描画命令（画像・図形）を「アンカー」として収集し、近接したものをクラスタリング。  
  - 近いテキストを吸収して矩形を拡張し、図版＋ラベルを一体で切り出す。  
  - 背景や極小、細線を除外し、テキストとの重なりもチェック。
- **オペレーター優先**: `imageExtractor.ts`  
  - `paintImageXObject`系の描画命令から画像矩形を直接取り出し、テキスト重なり率が高いものを除外。  
  - 実際の画像XObjectに絞るため確度が高い。
- **簡易（旧式）**: `wcagFix.ts` の `detectImageRegions`  
  - Canvasをタイル分割し、分散・エッジ量で「画像っぽさ」を判定して連結。  
  - テキスト重なり率で除外するが、装飾を拾うことがある。

## `clusterExtractor.ts` の流れ（クラスタリング方式）
1. **テキストとアンカーの収集**  
   - `getTextContent()`でテキストの矩形を取得。  
   - `getOperatorList()`を走査し、`paintImageXObject`や図形描画（パス＋fill/stroke）の矩形をアンカーとして取得。  
   - 背景レベル（ページの90%以上）や極小（50px未満）を除外。
2. **アンカーのクラスタリング**  
   - アンカー同士が重なる、または20px以内に近接していれば結合。  
   - 結合を繰り返し、まとまりをクラスタにする。
3. **テキストの吸収**  
   - 各クラスタに対し、近接20px以内または交差するテキストを含める。  
   - 図版内のラベルやキャプションをクラスタに取り込む。
4. **切り出し**  
   - `page.render()`済みCanvasから、クラスタ矩形をそのまま`drawImage`でクロップ。  
   - DataURLとして返却。本文テキストはクラスタから離れているため含まれにくい。

## `imageExtractor.ts` の流れ（オペレーター優先）
1. `getTextContent()`でテキスト矩形を取得。  
2. `getOperatorList()`で `paintImageXObject` などを検出し、CTMから矩形を算出。  
3. 背景（90%以上）や極小を除外し、テキスト重なり率が5%以上なら除外。  
4. `page.render()`後のCanvasから矩形をクロップし、DataURLを返却。

## `detectImageRegions` の流れ（簡易タイル方式）
1. Canvasを14×20のタイルに分割し、テキストrunのあるタイルをマスク。  
2. 残りタイルの輝度分散・エッジ量から画像候補を判定。  
3. 連結成分を作り、面積が3%以上でテキスト重なりが少ないものを採用。  
4. 最大3領域までオフスクリーンCanvasに切り出して利用。

## パラメータ例
- 近接・クラスタ結合距離: 20px  
- テキスト吸収距離: 20px  
- 極小除外: 50px未満  
- 背景除外: ページ面積の90%以上  
- テキスト重なり除外: 5%超で除外（imageExtractor）、クラスタでも同様のチェックを実装

## 現状の課題と改善余地
- 図形や装飾が多い資料では、簡易方式で誤検出することがある → クラスタリングorオペレーター方式を優先。  
- 低コントラストの図表や細線は拾いにくい → エッジ閾値調整やSobel等の導入余地。  
- パフォーマンス: 全ページで`render`＋`getOperatorList`が走るため、大きなPDFでは遅くなることがある → 必要ページのみ抽出する最適化を検討。***
